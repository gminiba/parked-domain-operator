---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: parkeddomains.parking.minibaev.eu
spec:
  group: parking.minibaev.eu
  names:
    kind: ParkedDomain
    listKind: ParkedDomainList
    plural: parkeddomains
    singular: parkeddomain
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ParkedDomain is the Schema for the parkeddomains API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ParkedDomainSpec defines the desired state of ParkedDomain.
            properties:
              domainName:
                description: DomainName is the fully qualified domain name to park.
                type: string
              region:
                type: string
              templateName:
                description: |-
                  TemplateName is the name of the template file (e.g., "index.html")
                  to copy from the configmap.
                type: string
            required:
            - domainName
            type: object
          status:
            description: ParkedDomainStatus defines the observed state of ParkedDomain.
            properties:
              nameServers:
                description: NameServers are the authoritative nameservers for the
                  zone.
                items:
                  type: string
                type: array
              status:
                description: Status indicates the current state, e.g., "Provisioned",
                  "Error".
                type: string
              zoneID:
                description: ZoneID is the ID of the created Route 53 Hosted Zone.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
